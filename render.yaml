services:
  - type: web
    name: leadscout-verifier
    env: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: node server.js
    autoDeploy: true
    healthCheckPath: /health
